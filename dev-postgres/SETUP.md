To use it just do
The server is automatically configured to use this server.

sudo docker compose up -d

or docker-compose

To connect via gui:
localhost:5050

- hostname: postgres
- db: purbank
- username: purbank_user
- password: dev_password


